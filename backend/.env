# ─── MongoDB ───
MONGO_URI=mongodb+srv://ehealthshield_db_user:IdckLrd7CfR1jZU0@cluster0.d0yh9pv.mongodb.net/?appName=Cluster0
MONGO_DB_NAME=e_healthshield

# ─── SSE Key (base64-encoded 32-byte key) ───
# Generate a new one: python -c "import os,base64; print(base64.b64encode(os.urandom(32)).decode())"
# Leave empty to auto-generate on first run (NOT recommended for production)
SSE_KEY=gQNppdoNpT+WABjxKzJqTizqI2lbUHhIsFOqHTA89LI=

# ─── Blockchain (placeholder — will be updated after smart contract deployment) ───
CONTRACT_ADDRESS=
BLOCKCHAIN_RPC_URL=http://127.0.0.1:8545

# ─── File Upload ───
MAX_FILE_SIZE_MB=10
